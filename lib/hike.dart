import 'package:flutter/material.dart';

class Hike extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(padding: EdgeInsets.all(20.0), child: Text('Hike Page'));
  }
}
